Outside of the working directory (maybe cd ..)
```
python -m venv tennis_env
source tennis_env/bin/activate
pip install -r requirements.txt
```

Inside the working directory
```
chmod +x tennis.sh
./tennis.sh
```